includes: ['layer:basic', 'layer:apt']  # if you use any interfaces, add them here
options:
  apt:
    packages:
      - gridengine-master
      - gridengine-client
      - gridengine-common
      - gridengine-exec
      - gridengine-qmon

