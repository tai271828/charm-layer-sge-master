includes: ['layer:basic', 'layer:apt']  # if you use any interfaces, add them here
options:
  apt:
    packages:
      - gridengine-client
      - gridengine-common
      - gridengine-master
      - gridengine-qmon
      - gridengine-exec

